org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
# Do not auto-download NDK/SDK packages (Program Files SDK is not writable; avoid long-path NDK unzip failures).
android.builder.sdkDownload=false
# Pub cache on C: + project on E: breaks Kotlin incremental relocatable paths.
kotlin.incremental=false
# AGP8 R8 full mode strips Gson TypeToken signatures used by flutter_local_notifications.
android.enableR8.fullMode=false
