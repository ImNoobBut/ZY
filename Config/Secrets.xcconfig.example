// Secrets placeholders — copy Secrets.xcconfig.example to Secrets.xcconfig
// and fill in real values. Secrets.xcconfig is gitignored.

SPOTIFY_CLIENT_ID =
// xcconfig treats // as a comment — use /$()/ to keep https:// intact
SPOTIFY_REDIRECT_URI = https:/$()/sleeping-routine-for-zy.pages.dev/callback
BACKEND_BASE_URL = http://127.0.0.1:8080
